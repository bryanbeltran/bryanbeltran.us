---
title: 'Lessons from Migrating Observability from Splunk to Dynatrace'
seoTitle: 'Splunk to Dynatrace Migration Lessons'
date: '2026-05-15'
summary: 'What I learned leading my team’s Splunk-to-Dynatrace migration — standards, cost-model constraints, and getting engineers and support to actually use the new tooling.'
tags: ['observability', 'dynatrace', 'engineering']
draft: false
---

At Chewy I led my team’s migration from Splunk to Dynatrace. Splunk was a company-wide platform — a $2M annual contract Chewy wanted to retire — and getting our team’s services fully onto Dynatrace was part of making that possible. The goal was not just swapping vendors; it was improving how our engineers debug production issues on the customer care platform.

I created the plan, defined the work, and did most of the implementation. A contractor joined near the end to help with instrumentation against our checklist.

This post captures what worked, what I would do differently, and the patterns that made adoption stick.

## Start with standards, not dashboards

The first temptation is to recreate every Splunk dashboard one-for-one. That recreates the mess.

We defined a small set of standards first:

- **Service health**: golden signals per service (latency, error rate, throughput)
- **Alert routing**: who gets paged, when, and for what severity
- **Dashboard tiers**: views accessible to tier 1 support vs engineering deep-dives

Once standards existed, I could implement consistently across our services instead of inventing a new layout per dashboard.

## The pricing model changes how you think

Splunk let us query as much data as we wanted for the same price. Dynatrace charges per gigabyte scanned.

That sounds like a billing detail. It became a design constraint.

Every dashboard, ad-hoc query, and alert evaluation had a cost implication we never had to weigh before. We had to teach people to scope queries, avoid runaway searches, and build views that answered the question without scanning more data than necessary.

That mental shift — from unlimited exploration to deliberate sampling — was as hard as the tooling change.

## Treat migration as a product rollout

Dynatrace only helps if people use it. I ran training sessions for engineers, tier 1 and tier 2 support, and the broader customer care tech team — not generic tool tours, but workflows mapped to real incidents they had already lived through.

Examples beat feature lists. Showing how to trace a failed CRM callback beats explaining "distributed tracing" in the abstract.

## What I would do earlier next time

- **Inventory alerts before migration.** Splunk had years of alert cruft. Auditing what actually fired (vs what someone set up in 2019) would have saved cleanup time.
- **Define "done" per service.** A checklist: dashboards, alerts, runbook link, on-call validation — prevented "we installed the OneAgent" from counting as migrated.
- **Teach the cost model early.** Per-GB scanning should be part of day-one training, not a surprise after people build expensive dashboards.
- **Measure adoption.** If people still ask for Splunk links, migration is not finished.

## Why this matters beyond one company

Observability migrations are as much about people and standards as they are about instrumentation and data pipelines. The tooling change is the easy part. Getting a team to trust new signals during incidents is the real work.

If you are mid-migration or planning one, start with standards and training — not parity dashboards. And if your new vendor meters by data scanned, treat that as an architecture decision, not a finance footnote.
